<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:JsonLoaderApp"
             x:Class="JsonLoaderApp.MainPage">

    <ListView x:Name="personListView" IsEnabled="True" IsVisible="True" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical" Padding="15">
                        <Label Text="{Binding Name}" TextColor="Black" FontSize="Large"></Label>
                        <Label Text="{Binding Email}" TextColor="Black" FontSize="Medium"></Label>
                        <Label Text="{Binding Company}" TextColor="Black" FontSize="Small"></Label>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
